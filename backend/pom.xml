<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <groupId>com.muzhou</groupId>
    <artifactId>muzhou-report</artifactId>
    <version>1.0.0</version>
    <name>muzhou-report</name>
    <description>木舟报表 - 拖拽式报表设计器 - 后端</description>

    <properties>
        <java.version>21</java.version>
        <mybatis-plus.version>3.5.7</mybatis-plus.version>
        <dynamic-datasource.version>4.3.1</dynamic-datasource.version>
        <aviator.version>5.4.3</aviator.version>
        <poi.version>5.3.0</poi.version>
        <hutool.version>5.8.32</hutool.version>
        <openpdf.version>3.0.5</openpdf.version>
        <zxing.version>3.5.3</zxing.version>
        <!--
          覆盖 spring-boot-starter-parent 管理的 Lombok 版本（3.3.5 给的是 1.18.34）。

          Lombok 靠反射摸进 javac 内部，**每出一个新 JDK 都要跟一版**；用比它新的 JDK 编译会在
          `java.lang.ExceptionInInitializerError: com.sun.tools.javac.code.TypeTag :: UNKNOWN`
          上整个挂掉 —— 不是某个文件编不过，是一行都编不了，报错也完全看不出跟 Lombok 有关。
          项目本身是 JDK 21（见上面的 java.version），但开发机上装的往往是更新的 JDK，
          所以这里锁一个新版本：它同时支持 21 与更高版本，两边都不必改环境。
          换更新的 JDK 又编不过时，先来这里升这个版本号。
        -->
        <lombok.version>1.18.46</lombok.version>
        <!--
          同理覆盖 Mockito 与它底下的 ByteBuddy（parent 3.3.5 给的是 5.11.0 / 1.14.19）。

          ByteBuddy 是靠**认得出 class 文件版本号**来改字节码的，JDK 出新版它就要跟一版；
          跟不上时 Mockito 造不出 mock，报的是「Mockito cannot mock this class」——
          看着像测试写错了，其实一行代码都没问题。两个一起升：只升 ByteBuddy 的话
          Mockito 5.11 未必配得上，只升 Mockito 的话 parent 的 dependencyManagement
          会把 ByteBuddy 钉回 1.14.19。
        -->
        <mockito.version>5.23.0</mockito.version>
        <byte-buddy.version>1.18.11</byte-buddy.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- MyBatis-Plus -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
            <version>${mybatis-plus.version}</version>
        </dependency>

        <!-- 动态数据源 -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>dynamic-datasource-spring-boot3-starter</artifactId>
            <version>${dynamic-datasource.version}</version>
        </dependency>

        <!-- Aviator 表达式引擎 -->
        <dependency>
            <groupId>com.googlecode.aviator</groupId>
            <artifactId>aviator</artifactId>
            <version>${aviator.version}</version>
        </dependency>

        <!-- Excel 导出 -->
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>${poi.version}</version>
        </dependency>

        <!--
            PDF 导出：OpenPDF(iText 4 的 LGPL/MPL 分支) 负责画 PDF，xlsx 侧仍然用上面的 POI 读。
            纯 Java，不需要在服务器装 LibreOffice / MS Office，也就没有外部进程要管。
        -->
        <dependency>
            <groupId>com.github.librepdf</groupId>
            <artifactId>openpdf</artifactId>
            <version>${openpdf.version}</version>
        </dependency>

        <!--
            条形码 / 二维码：ZXing 只取 core（纯 Java，Apache 2.0，无传递依赖）。
            官方另有 javase 模块，但它只多一个「点阵转 BufferedImage」的工具类
            （BarcodeGenerator 自己画了，还要顺带管文字），不值得把 jcommander 那串也拖进来。
        -->
        <dependency>
            <groupId>com.google.zxing</groupId>
            <artifactId>core</artifactId>
            <version>${zxing.version}</version>
        </dependency>

        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-all</artifactId>
            <version>${hutool.version}</version>
        </dependency>
        <dependency>
            <groupId>com.alibaba.fastjson2</groupId>
            <artifactId>fastjson2</artifactId>
            <version>2.0.47</version>
        </dependency>

        <!-- 驱动 -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
